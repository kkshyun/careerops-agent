---
task_id: PKB-008.1
review_round: 1
reviewer: claude
reviewed_at: 2026-08-22T17:10:00+09:00
verdict: PASS
---

## Scope of this round

`.ai/tasks/PKB-008.1-improve-extraction-quality.md`의 Root Cause/Scope
1-5/Acceptance Criteria(`[자동]` 항목 전부, `[수동]` 2건은 제외)/Technical
Notes를 전체 정독하고, 아직 커밋되지 않은 working tree diff(`git status`/
`git diff`)를 실제 코드 기준으로 검토했다. Codex thread
`01a02862-8baa-7620-b470-56e5aad5cc7e`의 round 1(및 표에 아직 반영되지
않은 컴파일/assertion 수정 왕복 포함) 결과물이 대상이다.

## Acceptance Criteria 체크 (`[자동]` 전부)

- [x] **SDK schema 유도: optional 필드가 `["...", "null"]` + `required`
  포함** — `StructuredExtractionSchemaTest.sdkSchemaMakesEveryOptionalNonListFieldNullableButStillRequired`
  (`StructuredExtractionSchemaTest.java:20-42`)가 5개 DTO의 optional
  필드 전체를 대상으로 실제 SDK `outputConfig(Class)` schema 유도 경로를
  호출해 검증한다. **직접 재현**: `/tmp/SchemaDump.java`를 작성해 동일한
  `MessageCreateParams.builder()...outputConfig(EducationCreateRequest.class)`
  경로를 별도 실행한 결과, `major`/`description`/`gpa` 등은
  `"type":["string","null"]`(또는 `["number","null"]`)로, enum 필드
  `degree`/`status`는 `"anyOf":[{"type":"null"},{"type":"string","enum":[...]}]`
  형태로 나타났고 두 형태 모두 `"required"` 배열에 필드명이 포함되어
  있음을 육안으로 재확인했다. `declaresNullType()`이 `findValues("type")`으로
  중첩된 `anyOf` 구조까지 재귀 탐색하므로 enum 필드의 nullable 표현
  차이를 정확히 처리한다(태스크가 언급한 "enum 필드 anyOf 이슈"가 최종본에서
  올바르게 해결됨을 독립 재현으로 확인).
- [x] **필수 도메인 필드는 여전히 non-nullable** —
  `sdkSchemaKeepsRequiredDomainFieldsNonNullable`(같은 파일 44-61줄)이
  `type`/`title`/`name`/`institution`/`content`를 검증. 위 독립 재현에서도
  `institution`이 `"type":"string"`(null 미포함)이면서 `"required"`에
  포함됨을 재확인했다.
- [x] **fake client 기반: sparse Certification/Education/Award도 candidate
  생성** — `ImportBatchExtractionServiceTest.createsMultipleOptionalSparseCertificationEducationAndAwardCandidates`
  (`ImportBatchExtractionServiceTest.java:134-153`)가 issuer/major 등
  일부만 채워진 Certification 2건 + Education 2건 + Award 2건을 반환해도
  `createdCandidateCount == 6`, `counts()`가 타입별 2건씩임을 확인.
- [x] **`ExtractionPromptBuilderTest` v2 assertion** —
  `promptV2CoversEveryTargetIndependentlyAndForbidsInferredDatesAndTags`
  (`ExtractionPromptBuilderTest.java:22-33`)가 4개 targetType 독립 탐색
  지시, "2025년 여름"/"2025.07" 날짜 null 예시, tags 명시적 언급 원칙
  문구를 assert. 기존 injection 방지 테스트
  `keepsUntrustedDocumentInsideTaggedUserMessage`(12-19줄)는 수정 없이
  그대로 유지됨(회귀 없음).
- [x] **`MAX_TOKENS == 16_000`** —
  `usesExpandedOutputTokenBudget`(`AnthropicDocumentExtractionClientTest.java:20-23`),
  실제 상수도 `AnthropicDocumentExtractionClient.java:23`에서 `16_000`으로
  확인.
- [x] **stopReason 처리, 로그 미노출** —
  `distinguishesTokenTruncationWithoutLoggingResponseOrInput`
  (`AnthropicDocumentExtractionClientTest.java:26-42`)이 Logback
  `ListAppender`로 ROOT logger를 감시하며
  `rejectTruncatedResponse(StopReason.MAX_TOKENS)` 호출 시 로그가 전혀
  없음(`appender.list`가 empty)과 `LlmExtractionException`의 cause가
  `OutputTruncatedException`(메시지는 `stopReason.asString()`인
  `"max_tokens"`뿐, 원문/응답 텍스트 없음)임을 확인.
  `rejectTruncatedResponse`(`AnthropicDocumentExtractionClient.java:80-86`)
  구현도 enum 값만 예외 메시지로 사용해 원칙과 일치.
- [x] **`DocumentChunkerTest`: 20,000자 미만 → chunk 1개** —
  `returnsSubThresholdDocumentUnchanged`(`DocumentChunkerTest.java:12-16`).
- [x] **`DocumentChunkerTest`: 20,000자 이상 → 여러 chunk, 손실/overlap 없음** —
  `splitsLongDocumentOnParagraphBoundariesWithoutLossOrOverlap`
  (17-28줄). 텍스트 길이를 직접 계산해 33,010자(임계값 20,000자 초과)임을
  확인했고, `String.join("", chunks) == text` 로 정보 손실/중복 없음을
  검증.
- [x] **`DocumentChunkerTest`: 목표 크기 초과 단일 문단 강제 분할** —
  `forceSplitsOnlyOversizedParagraphAndPreservesSurroundingParagraphs`
  (30-43줄). 알고리즘을 수동 트레이스한 결과 `before`/`after` 문단은
  그대로 보존되고 24,102자 문단만 12,000/12,000/102자 3조각으로 분할되어
  테스트 assertion과 정확히 일치함을 확인.
- [x] **`ChunkingDocumentExtractionClientTest`: sub-threshold → delegate
  1회, 원문 그대로** —
  `delegatesSubThresholdInputExactlyOnceAndUnchanged`(17-25줄).
- [x] **N개 chunk → delegate N회, 4개 리스트 chunk 순서대로 concat** —
  `concatenatesAllFourTypesInChunkOrder`(27-47줄), 실제 chunk 개수(3개)와
  입력 순서를 `chunker.chunk(text)` 결과로 직접 재구성해 검증.
- [x] **chunk 실패 시 즉시 전파, 후속 chunk 미호출** —
  `propagatesFailureImmediatelyWithoutCallingLaterChunks`(49-58줄)이
  `delegate.inputs`가 정확히 2건(성공 1 + 실패 1)임을 확인 —
  `ChunkingDocumentExtractionClient.extract()`(29-42줄)의
  `stream().map(...).toList()`가 순차 스트림에서 mapper 예외 시 이후
  원소를 평가하지 않는다는 Java 표준 동작과 일치.
- [x] **완전히 동일한 객체만 dedup, 다른 객체는 보존** —
  `removesOnlyCompletelyEqualRecordsAndPreservesFirstOccurrenceOrder`
  (60-74줄). `merge()`(44-49줄)가 `record`의 자동 `equals()`에 의존하는
  `distinct()`만 사용(semantic dedup 아님) — `StructuredExtractionResult`
  및 4개 DTO가 전부 순수 record(정규화 compact constructor만 있음)라 deep
  equals가 정상 동작함을 소스로 확인.
- [x] **`PlaceholderValueSanitizerTest`: `-`/`TBD`(대소문자 포함) 추가,
  회귀 없음** — `replacesExactPlaceholdersIgnoringWhitespaceAndCase`가
  `"-"`/`"TbD"` 케이스로 갱신(`PlaceholderValueSanitizerTest.java:17-27`),
  `doesNotReplacePartialMatches`(29-35줄)는 그대로 유지되어 "부분
  일치는 치환하지 않는다"는 기존 회귀 커버리지가 보존됨.
- [x] **`ImportBatchExtractionServiceTest`/`ImportBatchControllerTest`
  회귀 없음** — 전자는 `@MockitoBean DocumentExtractionClient`로 교체된
  것 외 기존 6개 테스트 케이스 로직 불변(assert 문구까지 동일, prompt
  version만 `"v1"→"v2"`로 갱신), 후자는 파일 자체가 미수정. 실행 결과
  6/6, 8/8 통과(아래 테스트 결과 참고).
- [x] **기존 `AnthropicDocumentExtractionClientTest` 오류 분류 회귀** —
  `classifiesTimeoutProviderErrorsAndMalformedResponses`,
  `missingKeyAndSensitiveInputAreNotLogged` 모두 수정 없이 유지, 통과.
- [x] **`match` 패키지 회귀 없음** — `git diff`/`git status`에 `match`
  패키지 파일이 전혀 등장하지 않음(수정 자체가 없음). 실행 결과
  `CareerMatchEngineTest`/`JobMatchControllerTest`/`KeywordNormalizerTest`
  10/10 통과.
- [x] **`career`/`job`/`application`/`collector`/`pkbimport` 전체 회귀
  없음** — 패키지별 집계: career 40/40, job 16/16, application 26/26,
  collector 33/33, pkbimport 66/66, manualimport 16/16, match 10/10 (전부
  실패/에러 0). `career/dto/*` 5개 파일 외 `career` 패키지의 다른 파일은
  전혀 수정되지 않음(`git diff --stat`으로 확인).
- [x] **rawText/API key/candidate payload 로그 미노출(chunking 신규
  로깅 지점 포함)** — `grep -rn "log\.\|Logger\|System\.out\|System\.err"
  src/main/java/com/careerops/backend/pkbimport/` 결과 0건 — 로깅
  자체가 존재하지 않음(`ChunkingDocumentExtractionClient`/`DocumentChunker`
  포함). `OutputTruncatedException` 메시지도 `stopReason.asString()`
  (`"max_tokens"` 같은 enum 문자열)뿐, 원문/응답 텍스트 없음.
- [x] **`cd backend && ./gradlew test` 전체 실패 0건** — 아래 테스트
  결과 참고.

`[수동]` 2건은 지시에 따라 이번 라운드 판정에서 제외했다(오케스트레이터가
실제 사용자 문서로 별도 진행 예정).

## Out of Scope 준수 확인

`git status`/`git diff --stat` 기준 변경 파일은 `build.gradle`,
`career/dto/{Award,CareerExperience,Certification,Education,ExperienceBullet}CreateRequest.java`
5개, `pkbimport/extraction/llm/*`(기존 3개 수정 + 신규 2개:
`DocumentChunker`, `ChunkingDocumentExtractionClient`),
`application.yml`, `docs/DECISIONS.md`, 그리고 대응 테스트 파일들뿐이다.

- `match` 패키지, `ImportCandidateService`/`ImportCandidateController`,
  `SourceDocument*`, `ImportBatchController`, `POST
  /api/career/imports/batches/{id}/extract` 요청/응답 스키마 — 전부 미수정
  확인(코드/테스트 diff에 등장하지 않음).
- 모델(`claude-sonnet-5`) 미변경, OCR/embedding/reranking/semantic
  dedup 코드 없음, DB migration 파일 변경 없음.

## SDK/DTO 정확성 재검증 (직접 재현)

- `org.jspecify:jspecify:1.0.0`이 `build.gradle:32`에 **`implementation`**
  으로 선언되어 있음을 직접 확인(`compileOnly` 아님 — 런타임 리플렉션
  요구사항 충족).
- `org.jspecify.annotations.Nullable`의 실제 jar(`~/.gradle/caches/...
  /jspecify-1.0.0.jar`)를 `javap -v`로 직접 디컴파일해
  `@Target(TYPE_USE)`, `@Retention(RUNTIME)`을 재확인(Technical Notes의
  주장과 일치).
- `/tmp/SchemaDump.java`를 별도 작성해 `EducationCreateRequest`/
  `ExperienceBulletRequest`에 대해 실제 Anthropic SDK 2.54.0의
  `outputConfig(Class)` schema 유도 결과를 직접 출력·검증(위 AC 체크
  1번 근거). 이 재현은 `StructuredExtractionSchemaTest`가 검증하는
  메커니즘 자체가 실제 SDK 동작과 정확히 일치함을 테스트 코드를 신뢰하지
  않고 독립적으로 증명한다.

## docs/DECISIONS.md ADR 확인

`ADR-0027`(`docs/DECISIONS.md`, 이번 diff로 신규 추가)이 Scope 1-5의
결정(nullable schema/prompt v2/token 예산/좁은 chunking)을 정확히
요약하고, 기각한 대안 3가지(별도 extraction 전용 DTO, 모든 문서
chunking/overlap, semantic dedup·모델 변경)를 명시한다. 특히 "별도
extraction 전용 DTO 도입" 기각 사유("기존 create DTO와 필드/validation
이중 관리로 drift 발생, 최소 변경으로 충분")가 Task 명세의 판단과
일치한다.

## 테스트 결과

- 사전조건 확인: `docker compose ps` → `postgres`/`redis` 모두
  `Up ... (healthy)`.
- `cd backend && ./gradlew test --rerun` 독립 재실행 → **BUILD
  SUCCESSFUL**.
- `build/test-results/test/*.xml` 전체 집계(49개 테스트 클래스):
  **test_count = 208, test_pass_count = 208(실패 0, 에러 0, skip 0)**.
- 패키지별 집계(전부 실패/에러 0): `application 26`, `career 40`,
  `collector 33`, `job 16`, `manualimport 16`, `match 10`,
  `pkbimport 66`, 기타 `1`. 합계 208로 전체와 일치.
- 신규/수정 테스트 클래스 개별 확인: `StructuredExtractionSchemaTest`
  (2/2), `DocumentChunkerTest`(3/3), `ChunkingDocumentExtractionClientTest`
  (4/4), `AnthropicDocumentExtractionClientTest`(4/4, 신규 2 포함),
  `ExtractionPromptBuilderTest`(2/2, 신규 1 포함),
  `PlaceholderValueSanitizerTest`(2/2), `ImportBatchExtractionServiceTest`
  (6/6, 신규 1 포함) — 전부 통과.

## Findings

없음(코드 결함/Acceptance Criteria 위반/원칙 위반 모두 발견되지 않음).
다음은 판정에 영향을 주지 않는 관찰 사항:

- 절차적 사항(코드 문제 아님): Task 명세의 "Codex Thread 기록" 표에는
  아직 round 1만 1줄 기록되어 있으나, 실제로는 컴파일 실패(존재하지
  않는 synthetic API 호출)와 assertion 실패(enum nullable의 `anyOf`
  표현) 수정을 위한 추가 왕복이 같은 thread에서 있었다. 최종 코드는
  두 문제 모두 올바르게 해결된 상태로 확인됐으므로 판정에는 영향이
  없지만, 오케스트레이터가 Task 명세의 표를 실제 왕복 횟수에 맞춰
  갱신할 필요는 있다(문서 정확성 문제).
- `ChunkingDocumentExtractionClient`가
  `com.careerops.backend.career.dto.*` 와일드카드 import를 사용한다 —
  스타일 사소한 관찰(기능/원칙에는 영향 없음).

## 다음 액션

- **PASS**. `[자동]` Acceptance Criteria 18개 전부 충족, 회귀 208/208
  통과(match 패키지 10/10 포함), Out of Scope 항목 전부 미침범,
  `implementation` dependency 선언 및 신규 로깅 지점 privacy 원칙 준수
  확인. ADR-0027이 결정과 기각 대안을 정확히 기록.
- Codex에게 추가로 보낼 수정 요청 없음.
- 후속 조치 제안(Codex 위임 범위 아님, 오케스트레이터 처리):
  1. Task 명세(`PKB-008.1-improve-extraction-quality.md`) frontmatter의
     `status: in_progress`는 `[수동]` 2건(사용자 실제 문서 E2E)이 완료된
     뒤 `done`으로 갱신.
  2. `.ai/tasks/PKB-008.1-improve-extraction-quality.md`의 "Codex Thread
     기록" 표에 컴파일/assertion 수정 왕복을 반영(위 Findings 참고).
  3. `.ai/metrics/metrics.jsonl`에 이번 라운드 결과(round=1[구현 완료
     기준], test 208/208, verdict=PASS) 기록.
  4. `[수동]` AC 2건(실제 이력서/경험정리로 `/extract` 재실행) 진행.
