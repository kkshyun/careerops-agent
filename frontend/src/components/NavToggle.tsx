"use client";
import {usePathname} from "next/navigation";import {useState} from "react";import {Bell,BriefcaseBusiness,Files,LayoutDashboard,Menu,UserRound,X} from "lucide-react";import Link from "next/link";import styles from "./Shell.module.css";
const links=[{href:"/dashboard",label:"Dashboard",icon:LayoutDashboard},{href:"/jobs",label:"채용공고",icon:BriefcaseBusiness},{href:"/applications",label:"지원현황",icon:Files},{href:"/career",label:"커리어",icon:UserRound},{href:"/notifications",label:"알림",icon:Bell}];
export function Navigation({onNavigate}:{onNavigate?:()=>void}){const path=usePathname();return <nav className={styles.nav} aria-label="주요 메뉴">{links.map(({href,label,icon:Icon})=><Link key={href} href={href} onClick={onNavigate} className={`${styles.navLink} ${path.startsWith(href)?styles.active:""}`}><Icon size={18}/><span>{label}</span></Link>)}</nav>}
export default function NavToggle(){const [open,setOpen]=useState(false);return <><button className={styles.menuButton} onClick={()=>setOpen(true)} aria-label="메뉴 열기" aria-expanded={open}><Menu size={22}/></button>{open&&<div className={styles.overlay} onClick={()=>setOpen(false)}><aside className={styles.drawer} onClick={e=>e.stopPropagation()}><div className={styles.drawerHead}><span>CAREEROPS</span><button onClick={()=>setOpen(false)} aria-label="메뉴 닫기"><X size={20}/></button></div><Navigation onNavigate={()=>setOpen(false)}/></aside></div>}</>}
